//fatorial n recursivo
int fatorial(int n)
{int i, fat=1
for (i=1;ic=n;i++);
fat=fat*1;
return fat;
}

fatorial recursivo 
int fatorial-rec(int n)
{int fat;
if(n=1)return 1;
else
return(n*fatorial-rec(n-1));
}
